plugins {
	id("qsl.module")
}

qslModule {
	moduleName = "item_setting"
	version = "1.0.0"
	library = "item"
	coreDependencies([
			"qsl_base"
	])

	coreTestmodDependencies([
	        "resource_loader"
	])
}
