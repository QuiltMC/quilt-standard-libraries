plugins {
	id("qsl.module")
}

qslModule {
	moduleName = "command"
	version = "1.0.0"
	library = "command"
	moduleDependencies {
		core {
			api("qsl_base")
			impl("resource_loader")
			compileOnly("networking")

			testmodOnly("lifecycle_events")
			testmodOnly("networking")
		}
	}
}
