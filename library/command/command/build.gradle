plugins {
	id("qsl.module")
}

qslModule {
	moduleName = "command"
	version = "1.0.0"
	library = "command"
	coreDependencies([
			"qsl_base",
			"resource_loader"
	])
	coreTestmodDependencies([
	        "networking"
	])
}

dependencies {
	// OPTIONAL dependency on networking
	compileOnly project(path: ":core:networking", configuration: "dev")
}
