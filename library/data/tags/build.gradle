plugins {
	id("qsl.module")
}

qslModule {
	moduleName = "tags"
	version = "1.0.0"
	library = "data"
	moduleDependencies {
		core {
			qsl_base {
				testmod = true
			}
			lifecycle_events {
				testmod = true
			}
			resource_loader
		}
		command {
			command {
				testmod = true
			}
		}
	}
}

loom {
	accessWidenerPath = file("src/main/resources/quilt_tags.accesswidener")
}
