plugins {
	id("qsl.module")
}

qslModule {
	moduleName = "registry_dicts"
	version = "1.0.0"
	library = "data"
	coreDependencies([
			"qsl_base",
			"lifecycle_events",
			"resource_loader",
			"networking"
	])
	intraLibraryDependencies([
			"tags"
	])
	interLibraryTestmodDependencies("command", [
	        "command"
	])
}

dependencies {
	// OPTIONAL dependency on command library
	compileOnly project(":command:command");
}
