plugins {
	id("qsl.module")
}

qslModule {
	moduleName = "registry_dicts"
	version = "1.0.0"
	library = "data"
	coreDependencies([
			"qsl_base", // this *should* be implicitly added by the below dep, but isn't...
			"resource_loader"
	])
	interLibraryDependencies([
			"tags"
	])
}
