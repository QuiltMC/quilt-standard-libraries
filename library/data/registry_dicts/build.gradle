plugins {
	id("qsl.module")
}

qslModule {
	moduleName = "registry_dicts"
	version = "1.0.0"
	library = "data"
	coreDependencies([
			"qsl_base",
			"resource_loader"
	])
	intraLibraryDependencies([
			"tags"
	])
}
