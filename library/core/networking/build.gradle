plugins {
	id("qsl.module")
}

qslModule {
	moduleName = "networking"
	version = "1.0.0"
	library = "core"
	moduleDependencies {
		core {
			api("qsl_base")
			api("lifecycle_events")
		}
		command {
			testmodOnly("command")
		}
	}
}
